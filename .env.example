# Copy to apps/host/.env.local or apps/controller/.env.local as needed. All optional.
# VITE_WS_URL=wss://xxx.ngrok.app/ws        # relay, if not same-origin /ws
# VITE_CONTROLLER_URL=https://xxx.ngrok.app/ # what the host QR encodes (host app only)
# PORT=8787                                   # relay port (server)
